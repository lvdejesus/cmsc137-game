package client.components;

import framework.engine.Component;

/** Tag component — every bullet entity has this, used by BulletSystem to query. */
public class BulletTagComponent implements Component {
}

Use the ![LWJGL generator](https://www.lwjgl.org/customize) to generate library files in lib/ with the provided config.
To use the Makefile build, extract the natives.

## Running

### On Windows

```
mingw32-make -f Makefile.win run
```

### On Linux

```
make run
```
